/* class Test{
  getTest(id) {
    this._id = id;
  }
}

let test = new Test(11)

console.log(test) */

let attr = 'backgroundColorName'

/* let result = attr.replace(/[A-Z]/g, function (matched) {
  return `-${matched.toLowerCase()}`
}) */
let result = attr.replace(/[A-Z]/g, matched => {
  return `-${matched.toLowerCase()}`
})


console.log(result)

switch (key) {
  case value:
    
    break;

  default:
    break;
}